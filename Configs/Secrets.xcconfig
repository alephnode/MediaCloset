//
//  Secrets.xcconfig
//  MediaCloset
//
//  This file defines the structure for secrets but should not contain actual secrets.
//  Actual secrets should be in Local.secrets.xcconfig (which is gitignored)
//

// These will be overridden by Local.secrets.xcconfig if it exists
GRAPHQL_ENDPOINT = 
HASURA_ADMIN_SECRET = 
OMDB_API_KEY = 